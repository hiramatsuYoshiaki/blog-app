rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /posts/{id} {
      allow read, write: if request.auth.uid != null;
    }
    match /stages/{id} {
      allow read, write: if request.auth.uid != null;
    }
    match /locations/{id} {
      allow read, write: if request.auth.uid != null;
    }
    match /tags/{id} {
      allow read, write: if request.auth.uid != null;
    }
  }
}